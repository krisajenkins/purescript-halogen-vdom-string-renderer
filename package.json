{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build -- --censor-lib --strict"
  },
  "devDependencies": {
    "pulp": "^13.0.0",
    "purescript": "0.12.5",
    "purescript-psa": "^0.5.1",
    "rimraf": "^2.6.1"
  },
  "dependencies": {
    "spago": "^0.9.0"
  }
}
